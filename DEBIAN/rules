#!/usr/bin/make -f
#export DH_VERBOSE=1
export PYBUILD_NAME=master
export PYBUILD_INSTALL_ARGS=--install-lib=/usr/share/master/ --install-scripts=/usr/share/master/

%:
	dh @$ --with python3 --buildsystem=pybuild
