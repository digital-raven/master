#!/usr/bin/make -f
#export DH_VERBOSE=1
export PYBUILD_NAME=work
export PYBUILD_INSTALL_ARGS=--install-lib=/usr/share/work/ --install-scripts=/usr/share/work/

%:
	dh @$ --with python3 --buildsystem=pybuild
